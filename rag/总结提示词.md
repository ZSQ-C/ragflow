请基于我提供的代码 / 代码流程描述，生成一份流畅易懂、逻辑闭环、底层溯源、可直接复现的结构化代码总结，严格遵守以下规则，禁止碎片化罗列，必须用连贯文字讲解流程与功能关系：
一、核心总览（带逻辑关系）
核心定位：用 1-2 段话讲清代码完整核心功能、适用场景、解决的业务问题；
整体流程串讲：用连贯文字梳理从头到尾的完整执行链路，标注关键底层 API / 模块，同时说明：
各个模块之间的调用关系（谁先执行、谁调用谁）；
每一步执行的目标，不是单纯罗列步骤。
二、模块拆分（固定顺序 + 关系说明）
严格按顺序拆分：初始化模块 → 核心入口方法模块 → 分支逻辑方法模块 → 具体实现方法模块 → 辅助方法模块
每个模块开头必须加一句话：说明该模块的作用、在整体流程中的位置、与其他模块的配合关系。
三、方法详细解析（强制 5 要素 + 【文字流程串讲】，核心优化！）
对每个方法，先用连贯文字完整讲解内部执行流程，再补充结构化信息，必须包含：

1. 方法文字流程串讲（解决死板痛点）
   完整描述方法从开始到结束的所有步骤；
   分支判断必须详细解读：如果满足XX条件（入参/状态）→ 执行XX功能 → 实现XX目标；如果不满足 → 执行XX逻辑 → 处理XX场景；
   讲清内部功能的配合关系、每一步操作的目的；
   说明异常 / 边界场景的处理逻辑和原因。
2. 强制 5 要素（结构化补充）
   入参：类型、必填 / 选填、默认值；
   核心逻辑：精简总结；
   输出形式：返回值、空值 / 异常返回规则；
   底层关键依赖：核心模块 / 原生 API（如文件读取靠open()）；
   关键代码片段：精简核心代码。
3. 特殊处理标注
   高亮标注编码、异常捕获、格式转换、边界判断等细节。
   四、同类逻辑对比表
   同类功能（多格式读取、多类型解析）用表格汇总，包含：功能名称、核心流程、入参、底层依赖 API、输出格式、差异场景。
   五、疑惑解答
   结合底层 API + 代码流程 + 业务目标，用文字详细解释我标注的疑问。
   六、规范修正
   修正笔误、统一专业术语，保持语言专业且通俗。
   七、可复现实操步骤（傻瓜式落地）
   按代码完整执行流程生成分步指南，每一步包含：
   操作内容 + 依赖 API / 模块 + 最简代码 + 注意事项 + 执行目标；
   零基础可直接照着复现完整功能。
   八、关键模块总览
   汇总所有核心依赖，标注：模块名称、负责功能、在流程中的核心作用。
   请上面代码总结模板对给出项目模板进行分析并将每个模板分析结果生成内容保存到对应的模板下，用md格式保存

