# RAGFlow 简历项目经历 · 面试追问与陷阱题库

> 本文件对应「企业级 RAG 知识库问答系统」简历中的 5 个通用模块（文档解析、混合检索、多租户权限、增量更新、评估闭环）。
> 每个问题按三段组织：**考察意图**（面试官在测什么）→ **参考答法**（真做过的人会怎么答）→ **露馅点**（这样答会暴露是背的/没真做过）。
> 陷阱题（标【陷阱】）专门揪"背技术名词但没上过生产"的候选人，务必逐题攻下。
> **分工定位**：核心负责「文档解析 + 混合检索」两个模块，参与「权限体系 + 评估闭环」搭建；「增量更新」为同事主导的边缘模块。
> **保留短板**：不必每题都答完美，边缘模块坦诚"同事主导、我配合接入、细节了解不深"反而更真实。量化数字请替换/确认成你自己的实测值。

---

## 一、简历五模块回顾（参照 · 含职责分工 / 量化结果 / 迭代过程）

> 定位：**核心负责文档解析与混合检索，参与权限体系和评估闭环搭建**；**增量更新为同事主导的边缘模块**。核心模块做到"能讲迭代过程 + 有量化数据"，边缘模块坦诚"配合接入、细节不深"更真实。

### 职责分工（贴合 1 年经验能力边界）

| 模块 | 角色 | 面试策略 |
| --- | --- | --- |
| 模块一 文档解析与语义分块 | **核心负责** | 深挖，讲迭代 + 数据 |
| 模块二 混合检索 + 重排序 | **核心负责** | 深挖，讲迭代 + 数据 |
| 模块三 多租户隔离 + 权限 | 参与（配合接入） | 中挖，讲清关键机制 |
| 模块四 增量更新 + 版本管理 | 边缘（同事主导） | 坦诚"配合接入，细节不深" |
| 模块五 评估体系 + badcase 闭环 | 参与 | 中挖，讲闭环思路 |

### 量化结果（每个模块 1-2 个，口播用；数字请替换成你自己的实测）

| 模块 | 量化结果 |
| --- | --- |
| 文档解析 | 优化乱码检测后，不必要 OCR 占比下降 60%，解析平均耗时降低 40% |
| 混合检索 | 加入 BM25 混合后，型号/缩写类查询 Top-5 命中率相对提升约 30% |
| 权限过滤 | 权限过滤前置后，无权内容进入上下文的案例降为 0 |
| 增量更新 | 增量向量化后，单文档更新处理耗时降低约 70% |
| 评估闭环 | 建立标注集对照后，MRR 从约 0.62 提升到 0.74 |

### 迭代过程（每个核心点："最初方案 → 问题 → 优化"）

1. **文档解析 · 分块**：最初按固定长度硬切 → 表格被切断、召回率低 → 改结构感知分块，表格类查询召回提升 15%。
2. **文档解析 · 乱码检测**：最初"乱码就 OCR"一刀切 → 误判多、又慢又不准 → 改多维启发式判定 + 分级回退，不必要 OCR 下降 60%。
3. **混合检索**：最初纯向量检索 → 型号/缩写命中差 → 加 BM25 加权融合 + 重排，精确类查询命中显著提升。
4. **权限控制**：最初生成后再过滤 → 敏感内容已进上下文（等于泄露）→ 改检索阶段 filter。
5. **增量更新**：最初每次更新全量重向量化 → 成本高 → 改 hash 变更检测 + 局部重算。

### 短板边界（保留真实感）

- 边缘模块（增量更新）背书话术："这块当时是同事主导的，我主要配合接入，了解核心是 hash 变更检测 + 增量重算 + 软删失效，但更细的实现细节我了解得不是特别深，可以展开讲我负责的检索侧如何配合缓存/索引失效。"
- 原则：核心模块（解析、检索）把迭代和数据讲透；参与模块讲清"关键机制 + 为什么"；边缘模块坦诚边界，绝不硬答。

---

## 二、模块一：文档解析与语义分块【核心负责 · 深挖】

### Q1【陷阱】扫描件和"字体子集化导致的乱码 PDF"是一回事吗？都直接丢去 OCR 吗？

- **考察意图**：最经典陷阱。多数人把"提取出乱码"偷懒归为"扫描件→OCR"，但两者本质不同。
- **参考答法**：扫描件**没有文本层**，页面是纯图像，只能 OCR；字体子集化乱码**有文本层**，但 ToUnicode/CMap 映射缺失，提取出的是 CID 码或 PUA 私有区字符。正确顺序：先判断"有没有文本层 + 文本是否有效"，字体子集化优先尝试**补全/修复字体映射**（便宜又准），只有真无文本层才 OCR。判断错了要么把扫描件当文字版提取出垃圾，要么把普通 PDF 误判去跑 OCR，又慢又不准。
- **露馅点**：直接说"乱码就 OCR"，或答不上 CID/PUA 是什么。

### Q2 你的"启发式乱码判定"具体怎么判？阈值怎么定？

- **考察意图**：验证是否真写了检测逻辑。
- **参考答法**：多维叠加，不是单阈值——① PUA/CID 占位字符占比；② 有效中文字符占比过低；③ 标点/替换符（``）密度异常；④ 提取文本量与页面预期字数的比严重偏低。阈值在真实文档集上调，目标是把误判率压到很低（OCR 有代价）。迭代：最初"乱码就 OCR"一刀切，误判多又慢又不准；改成多维判定 + 分级回退后，不必要 OCR 占比下降 60%、解析平均耗时降低 40%。
- **露馅点**：说不出具体维度，或只答"我用正则"。

### Q3 误把正常文档判成乱码去 OCR，会有什么后果？怎么兜底？

- **考察意图**：考失败成本意识 + 兜底设计。
- **参考答法**：又慢又可能更不准（OCR 比文本提取慢 5~8 倍，对清晰文字层未必更准）。兜底：检测结论带二级置信度，模糊地带先走文本提取再看质量；对 OCR 输出做字符有效性回流校验；判定结果记入元数据便于回溯调阈值。
- **露馅点**：答"没关系，OCR 反正能用"。

### Q4 PyMuPDF / pdfplumber / Camelot 都有，为什么自己写检测 + OCR 回退？

- **考察意图**：考是否理解现成库的边界和自己增量在哪。
- **参考答法**：现成库能提取**标准文字型 PDF**，但缺"这份文档该不该 OCR、该用哪个引擎"的**路由层**。我写的是检测 + 分流 + 结果校验这层胶水，不同文档类型路由到不同解析器，异常就回退。核心价值在路由和兜底，不在重复造轮子。
- **露馅点**：答"因为那些库不准"——没调研也没说清增量价值。

### Q5 跨页表格、合并单元格怎么处理？

- **考察意图**：表解析最难场景，真做与背流程的分界。
- **参考答法**：跨页表格靠坐标/边框重合度 + 表头重复特征判断是延续，合并后转 Markdown 保留行列；合并单元格按**栅格化**处理，先用横竖线/对齐位置恢复单元格网格再填内容，避免重复或错位。
- **露馅点**：只说"用库提取表格"，细节问不出来。

### Q6 无边框表格？表格转 Markdown 就够了吗？

- **考察意图**：继续压深。
- **参考答法**：无边框表靠文本块 x 坐标聚类成列、y 聚类成行恢复列对齐。转 Markdown 不是终点：复杂表会丢合并单元格的层级语义，下游 Prompt 还要带表头关联信息，否则模型读不懂"这数字属于哪行哪列"。
- **露馅点**：觉得"转成字符串丢给模型就行"。

### Q7【陷阱】"按标题层级语义分块"遇到没标题的文档（扫描件 OCR 后的纯文本）怎么办？

- **考察意图**：直击语义分块适用边界。
- **参考答法**：语义分块是**有结构时的最优解**，不是全场景。没标题时退化为按段落/长度 + 固定重叠；或小规模用 LLM 做语义边界切分（贵）。关键是分级策略：有标题走标题层级，没标题走 fallback，不是一个策略吃所有。
- **露馅点**：坚持"都能按标题切"。

### Q8 chunk 大小和重叠怎么定的？有实测吗？

- **考察意图**：验证是否真调过参。
- **参考答法**：在真实标注集上交叉验证，试 256/512/1024 和不同重叠（10%~15%），看召回/相关性拐点。重叠减少跨 chunk 边界截断，但过大带来重复上下文和成本。迭代：最初直接按固定长度硬切，发现表格被切断导致召回率低，后改成结构感知分块（表格/图片当原子单元），表格类查询召回提升约 15%。
- **露馅点**：背"500~1000、重叠 20%"，却说不出自己怎么验证。

### Q9 分块把一张表格切断了、或图片被单独切出去怎么办？

- **考察意图**：考结构保持。
- **参考答法**：表格/图片是**不可再分单元**，分块前先识别边界，当原子单元不被长度切断；图片要么走多模态读图，要么把 OCR 文本/标题附着到所属 chunk 保证上下文完整。
- **露馅点**：只讲纯文本分块，没意识到表/图会被切坏。

### Q10 怎么证明"解析这一步"没坏，而不是下游检索/生成的问题？

- **考察意图**：考质量评估与归因。
- **参考答法**：解析层单独设质量闸口——提取后校验（文本量、字符有效率、表格行列数）+ 抽样人工核对原文；出问题沿链路归因：chunk 干净但召回不对是检索问题，chunk 已是乱码/碎片才是解析问题。分层可观测才能定位。
- **露馅点**：说"解析一般不会出问题"。

### Q11【场景题】一份 1000 页 PDF 上传，怎么设计处理流程？

- **考察意图**：考异步/并发/进度/超时，最能暴露后端功底。
- **参考答法**：走异步任务管线——切页分片、多 worker 并发解析、信号量限流防内存爆；分块/向量化/入库分级并发；进度上报；页级失败隔离（单页坏不影响整体，标记重试）；超大文件限制 + 超时兜底。目标是上传后异步、可中断、可重试、可观测。
- **露馅点**：说"同步一页页读完解析"或没想并发内存。

---

## 三、模块二：混合检索 + 重排序【核心负责 · 深挖】

### Q12 向量检索和 BM25 各解决什么问题？什么时候只用其一？

- **考察意图**：考是否懂两者互补本质。
- **参考答法**：向量解决**语义泛化**（同义、改写、跨语言），BM25 解决**精确词匹配**（型号、编号、缩写、专有名词）。纯粹口语化 FAQ 可以偏向量；纯精确 ID 查询库可以偏 BM25；混合场景两者都要。多数企业知识库是混合场景，所以用融合。迭代：最初纯向量检索，型号/缩写类查询命中差，加入 BM25 加权融合 + 重排后，这类查询 Top-5 命中率相对提升约 30%。
- **露馅点**：答"向量更快"或纠结谁更准——没说到互补关系。

### Q13 融合怎么做？RRF 和加权求和（weighted_sum）区别？你用的哪种，为什么？

- **考察意图**：考融合细节。
- **参考答法**：加权求和用归一化分数乘权重相加，权重可调、直观；RRF 用**排名倒数**融合（1/(k+rank)），对分数分布不敏感、不用归一化，鲁棒但权重调节粒度粗。我用加权求和，因为能按库内容显式调 BM25/向量占比，且可解释。
- **露馅点**：说"都用"但讲不清区别和使用动机。

### Q14【陷阱】两个权重（如 0.95 向量 / 0.05 BM25）怎么来的？拍脑袋还是实测？

- **考察意图**：揪"参数是背来的"。
- **参考答法**：在标注集上做网格搜索/对照，看 Top-K 命中率拐点定权重；术语/编号多的库把 BM25 权重调高。**不是固定值**，是随语料特征调的。
- **露馅点**：报一个精确权重却说不出怎么验证的。

### Q15 Rerank 什么时候加？代价是什么？延迟受不了怎么办？

- **考察意图**：考权衡。
- **参考答法**：召回（recall）优先、重排（precision）收窄——先多路召回 N 条（如 64~128），再接 Cross-Encoder 精排取 Top-K。代价是额外延迟和算力。延迟敏感时可以只对 Top-N 做精排，或用轻量重排模型，或先无重排、效果不够再加。
- **露馅点**：答"反正都要加重排"，没意识到延迟成本。

### Q16【陷阱】降阈值重试会召回一堆噪音，怎么防止"为了不零结果牺牲相关性"？

- **考察意图**：考兜底的边界控制。
- **参考答法**：降阈值重试有下限，降到下限仍无效就明确返回"未找到"或走人工，而非硬凑答案；降阈值后的结果要**降权/标记低置信**，让生成阶段知道这是弱证据；用评估跟踪降阈后的相关度，避免兜底策略反噬质量。
- **露馅点**：答"一直降阈值总能召回"——没想过噪音污染。

### Q17 为什么用 ES 的 BM25，而不是向量库自带的全文/稀疏检索？

- **考察意图**：考选型合理性。
- **参考答法**：取决于是否已有 ES 基础设施和 BM25 的成熟度（分词可控、同义词、调参经验丰富）。如已有 ES，复用成本低；向量库自带的稀疏/全文相对新、生态和运维经验没 ES 成熟。核心是"少引入新组件 + 复用成熟能力"。
- **露馅点**：答"BM25 就得用 ES，别的都不行"。

---

## 四、模块三：多租户隔离 + 文档级权限（ACL）【参与 · 中挖】

### Q18【陷阱】权限过滤放在检索阶段 filter，还是生成之后判断？

- **考察意图**：陷阱中的陷阱——后置判断已经是事故。
- **参考答法**：必须**检索阶段 filter**。生成之后再判断，无权内容已经进了模型上下文，等于数据已经泄露；而且浪费 token、可能被模型无意复述。正确做法是 ACL 作为检索的硬过滤条件，无权内容根本进不来。
- **露馅点**：答"生成后再删掉敏感句"。

### Q19 过滤条件怎么带进向量检索？元数据过滤和"检索后再过滤"性能差多少？

- **考察意图**：考是否懂向量库过滤机制。
- **参考答法**：ACL 作为**标量元数据**随 chunk 入库，检索时带上过滤条件，在向量索引里做**预过滤**（或后过滤，看引擎实现）。检索后再过滤要付出"召回但全丢弃"的成本，还可能因召回不足导致漏答；预过滤在查询侧裁剪搜索空间，正确性优先。
- **露馅点**：分不清预过滤/后过滤，或不知道向量库怎么支持标量过滤。

### Q20【陷阱】用户权限在检索进行中变了（比如被撤销），会发生什么？

- **考察意图**：考权限一致性/缓存失效。
- **参考答法**：短请求内影响小，但要注意**权限缓存**——如果缓存了用户权限或检索结果，权限变更后必须失效对应缓存，否则撤销后仍能看到旧结果。关键是把权限当"会话/请求维度"实时拉取或用短 TTL，敏感系统宁可多查一次权限也不能用脏缓存。
- **露馅点**：答"不会发生"或没想到缓存一致性问题。

### Q21 多租户、多知识库，索引/存储层怎么隔离？

- **考察意图**：考隔离手段掌握程度。
- **参考答法**：逻辑隔离用 tenant_id/kb_id 作为每条记录必带的维度 + 检索强制过滤；高隔离需求做物理隔离（按租户分索引/分桶/分库）。对象存储按租户分桶，向量/ES 按租户分索引或至少带租户字段。核心是**默认拒绝**，每个查询都必须显式带上租户维度。
- **露馅点**：只答"加个 tenant 字段"，没提默认拒绝或物理隔离。

### Q22【陷阱】文档内容夹带"忽略系统指令"这类注入怎么办？注入和权限有什么关系？

- **考察意图**：串联反注入 + 权限，考安全意识。
- **参考答法**：知识库文档和用户输入一律当**数据（data）而非指令（instruction）**，系统 Prompt 明确划界，防止文档里的文字覆盖指令（比如"把所有权限文档都列出来"）。高风险工具分级授权 + 审计日志。权限是"谁能看"，反注入是"内容不能越权指挥系统"，两者都要在入口做隔离。
- **露馅点**：答"知识库内容是我们自己上传的，不会有问题"。

---

## 五、模块四：增量更新 + 版本管理【边缘 · 同事主导，可坦诚细节不深】

> 背书话术（这块同事主导）："增量更新这块当时是同事主导的，我主要配合接入，了解核心是 hash 变更检测 + 局部重算 + 软删失效，但更细的实现细节我了解得不是特别深，可以展开讲我负责的检索侧如何配合缓存/索引失效。"

### Q23 用 hash 识别变更，具体 hash 什么内容？改一个字符也会全量重向量化吗？

- **考察意图**：考变更检测粒度。
- **参考答法**：粒度决定成本——按整个文件 hash，改一个字就全重跑（粗粒度简单但浪费）；按章节/段落 hash 能定位到具体变更单元，做**局部重向量化**。成熟做法是"文件级 + 内容级"两层：文件级快速判断变没变，内容级定位哪一段变了。
- **露馅点**：只答"用 md5 判断"，没考虑粒度导致的成本。

### Q24【陷阱】增量更新后旧 chunk 和新 chunk 同时存在，检索到两个版本怎么办？

- **考察意图**：考版本一致性。
- **参考答法**：变更后旧版本立即 soft_delete（过滤掉），保证检索只命中当前生效版本；需要回滚/审计时保留旧版本但不参与检索。关键动作是**同一逻辑单元的旧块在写入新块时原子失效**，避免同一次查询同时命中新旧两个矛盾答案。
- **露馅点**：答"两个版本都在，让模型自己选"。

### Q25 soft_delete 和硬删除怎么选？什么时候必须硬删？

- **考察意图**：考软硬删权衡。
- **参考答法**：日常更新用 soft_delete（可回滚、可审计、误删能恢复）；涉及**合规/隐私**（如用户要求删除、数据必须物理清除）时必须硬删，并同步清掉对象存储原文、ES/向量里的内容和备份。软删要配合定期清理，否则堆积无效数据。
- **露馅点**：答"软删就够了，反正看不到"——漏了隐私合规场景。

### Q26【陷阱】文档删了，但用户检索的历史缓存/已生成引用还指向旧内容，怎么保证一致性？

- **考察意图**：考缓存失效 + 一致性闭环。
- **参考答法**：源数据更新即**失效对应缓存**（精确缓存按文档 ID，语义缓存按影响范围刷新）；已生成的引用无法回改，但可以在后续请求里基于新版本重新检索。核心是"变更事件要能触发下游缓存/索引的失效"，而不是靠用户刷新。
- **露馅点**：没想到缓存，或说"引用是历史记录不用管"。

### Q27 更新很频繁的文档，怎么避免频繁触发重建？

- **考察意图**：考工程成本优化。
- **参考答法**：合并/节流变更（短时间多次更新合并成一次重建）；只重算变更部分；把"高变更文档"和"稳定文档"分开调度，高变更的降低优先级或延迟批量处理；必要时用异步队列削峰。
- **露馅点**：答"每次更新都全量重建"。

---

## 六、模块五：评估体系 + badcase 闭环【参与 · 中挖】

### Q28 你的标注集怎么建的？多少条？ground truth 怎么定？

- **考察意图**：考评估是否真实落地。
- **参考答法**：从真实用户 query + 抽样构造，覆盖高频/长尾/多跳/精确号几类；几十到几百条起步（太小无统计意义）。ground truth 用"标准答案 + 必含关键证据的 chunk 集合"，标注人/模型标注后人工抽核。关键是有**分层分类型**，不是随机堆 query。
- **露馅点**：说"我造了几千条"却说不出怎么分类型、怎么核。

### Q29 Recall@K / MRR / Faithfulness 分别测什么？为什么检索和生成要用不同指标？

- **考察意图**：考指标理解。
- **参考答法**：Recall@K 看正确 chunk 是否进了 Top-K（召回能力）；MRR 看第一个正确答案排在第几位（排序质量，越靠前越好）；Faithfulness 看生成是否忠于检索到的证据（有没有编）。检索层用召回/排序指标，生成层用忠实度/相关性——因为层不同，失败模式和度量也不同，混用会误判。
- **露馅点**：答"都用 accuracy"。

### Q30【陷阱】离线指标涨了，线上用户就是觉得没变好，怎么回事？

- **考察意图**：考离线/在线 gap 的判断力。
- **参考答法**：可能——① 标注集与真实分布偏差（用户问的是长尾/口语，标注集偏理想化）；② 指标不敏感（MRR 涨不代表用户高频问题变好）；③ 生成层/延迟/体验问题被指标掩盖。解法：补齐真实 online 数据、看**分问题类型的指标**、加用户反馈（点赞/转人工）做在线评估，别只盯一个整体分。
- **露馅点**：答"那是用户感觉问题"。

### Q31【陷阱】"答案相关度低"怎么归因是切片、检索还是模型的问题？

- **考察意图**：考逐层归因能力，简历"badcase 闭环"的直接追问。
- **参考答法**：沿链路定位——① 正确 chunk 是否召回（没召回 → 检索/索引问题）；② 召回了是否进 Top-K（进了但排序靠后 → rerank/阈值问题）；③ 上下文对了但答案错（→ 模型/生成问题）；④ chunk 本身碎片或乱码（→ 解析/分块问题）。一层层看证据，不先甩锅模型。
- **露馅点**：直接答"模型不行"或"向量没召回到"却不给定位方法。

### Q32 LLM-as-Judge"自己评自己"，怎么保证可信？有哪些偏差？

- **考察意图**：考评估方法学，2026 高频。
- **参考答法**：偏差包括位置偏差（偏好首选项）、长度偏差（偏好长答案）、自评分偏高等。缓解：用更强的模型做 judge、双模型交叉、随机打乱选项顺序、给明确打分标准（rubric）、定期用人工标注校准 judge 的一致性。
- **露馅点**：答"LLM 评分很客观，直接用就行"。

### Q33 小团队没有真实标注数据，怎么起步做评估？

- **考察意图**：考务实落地。
- **参考答法**：先用**有标注的几个核心高频 case 起步**（哪怕几十条，覆盖黄金路径），配合 LLM-as-Judge 做粗评；随线上运行沉淀**用户反馈**（转人工、点踩、成功对话）当弱标注；关键是先建立"改动前后能对比"的最小闭环，再慢慢扩规模，而不是等完美数据再开工。
- **露馅点**：答"没数据就没法评估，等以后再说"。

---

## 七、通用答题框架（所有追问都能套）

面试官问任何技术点的底层逻辑，都是这四步：

1. **先讲上下文/触发条件**（在什么情况下会做这个、为什么现在必须做）；
2. **再讲怎么做**（真实机制，落到具体字段/算法/参数，而不是名词）；
3. **主动讲权衡**（成本、延迟、边界、失败兜底）；
4. **收一句结论**（一句话讲清这一步对整体效果的价值）。

**判断自己会不会穿帮的快捷自测**：如果一个问题你只能用"我用了 XX 库/XX 框架"来回答、而说不出库背后的机制和它的边界，就说明这条还不能放心写进简历——回去看源码或补一次真实实验。