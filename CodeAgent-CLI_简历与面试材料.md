# CodeAgent-CLI 简历与面试材料

> 定位：1 年左右大模型 / 后端开发经验，目标岗位「大模型应用开发 / RAG / Agent 开发」。
> 核心原则：**RAG 底层归中台、本人负责业务侧编排 + Agent 链路**；能力裁剪是主动权衡而非短板；流水线分析是主线场景；**不编造无实测的量化指标**。

***

&#x20;&#x20;

> \## 一、开发周期：绝对不能说“做了一整年” 一个部门级CLI MVP工具，做一整年既不符合工程效率，也会显得你产出极低。\*\*最贴合大厂内部试点工具的真实周期是：核心MVP开发2.5\~3个月，后续持续迭代优化4\~5个月，剩余时间穿插其他工作\*\*，整体时间线和你1年左右的工作经验完全匹配。 ### 标准分阶段时间线（面试可以直接讲） 1. \*\*需求调研 & 技术选型（约2周）\*\* 对齐部门研发的真实痛点，调研Minimax模型、内部中台RAG接口、MCP协议适配性，确定轻量Agent的技术方案，输出MVP范围。 2. \*\*核心MVP开发（约2.5\~3个月）\*\* 完成Agent调度引擎、工具接入抽象层、RAG上下文编排模块、流水线分析专用链路、CLI核心交互联调，跑通“输入命令→拉取数据→生成结论”的完整闭环，产出可用版本。 3. \*\*试点灰度 & 迭代优化（持续4\~5个月）\*\* 小范围开放给核心项目组试用，收集badcase，分层归因迭代；补充新增Skill、优化Prompt、调整成本策略、修复边界问题；逐步扩大试点范围。 4. \*\*日常运营 & 穿插其他工作（剩余时间）\*\* 负责工具的日常答疑、知识库更新、版本发布，同时参与部门其他AI工具的辅助开发、运维工具开发等工作。 > 核心逻辑：\*\*MVP快速跑通，长期小步迭代\*\*，这是内部创新工具的标准节奏。2\~3个月做出能用的版本，边用边改，才符合真实工作场景。 ## 二、一年只做这一个肯定单薄，补充这些职责最合理 补充原则：\*\*不喧宾夺主（核心还是CodeAgent）、符合大厂外包/业务工具团队的真实分工、和大模型/工具开发方向强相关、不编造硬核底层能力\*\*。 以下三类内容，完全符合你“华为客户端业务部门、内部AI工具组、运维转开发”的身份背景，加进去既饱满又真实。 ### 第一类：部门其他AI工具的协同开发（贴合“多个AI工具之一”的设定） 这是最顺的补充——你所在的小组不止做这一个CLI工具，你在主导CodeAgent之外，也参与了其他工具的部分模块开发，工作量刚好填满一年。 - 参与部门其他代码辅助工具的\*\*RAG接入与Prompt调优\*\*：比如IDE插件版代码助手的业务侧RAG适配、知识库同步、Prompt效果调优。 - 负责内部AI工具的\*\*统一知识库运营\*\*：统筹多工具共用的开发规范、接口文档类知识库的更新、清洗、分类，同步给不同工具的RAG链路。 - 参与工具的\*\*效果验证与badcase复盘\*\*：配合组内同学做横向对比测试，沉淀通用的问题排查方法论。 ### 第二类：运维/工具链相关开发（贴合你原有运维背景，过渡自然） 华为外包岗普遍会兼顾一部分运维、工具链支撑工作，这部分非常真实，也能体现你“从运维转大模型应用开发”的职业路径，不突兀。 - 负责部门内部\*\*运维脚本与效率工具开发\*\*：比如流水线批量操作脚本、环境部署自动化工具、日志查询小工具，提升研发日常效率。 - 参与CI/CD流水线的维护与优化：配合流水线团队做故障排查、规则优化，这也为你做「流水线分析Agent」积累了业务场景理解，逻辑闭环。 - 负责内部工具的\*\*打包发布与日常运维\*\*：CLI工具的版本打包、分发、线上问题排查，保障工具可用。 ### 第三类：日常支撑与沉淀（内部工具必然有的工作，细节拉满真实感） 这类工作不大，但非常能体现“真的在内部做工具”，而不是背了个项目。 - 编写工具使用文档、最佳实践，面向部门研发做内部小范围培训推广。 - 收集一线研发的需求反馈，做需求评估、优先级排序，输出迭代规划。 ## 三、简历里怎么写：主次分明，不杂乱 核心项目CodeAgent占70%篇幅，其他工作用「其他工作内容」模块简洁带过，既饱满又不分散重点。 ### 简历示例（工作经历模块补充版） > \*\*华为终端 · 大模型应用开发 / 研发工具岗\*\* | 外包 | XX年XX月 - 至今 > > 负责部门内部研发效率类AI工具的开发与迭代，核心主导CodeAgent-CLI代码智能助手的Agent链路与RAG应用层实现，同时参与多工具的知识库运营与效率工具开发。 > > \*\*核心项目：CodeAgent-CLI 代码智能助手（内部MVP试点）\*\* > （这里放之前优化好的项目描述，约200字） > > \*\*其他工作内容：\*\* > - 参与部门IDE侧代码助手的RAG接入适配与Prompt调优，同步维护多工具共用的内部研发知识库，完成文档清洗与分类治理； > - 开发多款运维效率脚本与小工具，覆盖流水线批量操作、日志查询、环境部署等场景，提升研发日常工作效率； > - 负责工具的版本发布、试点支持与需求收集，沉淀badcase分层归因方法，支撑工具持续迭代。 ## 四、面试口述话术（被问“你这一年都做了什么”） > “我入职初期先做了一段时间的研发效率工具和运维脚本开发，同时熟悉部门的研发流程和流水线体系。大概入职两三个月的时候，部门启动内部AI研发工具的试点，我就转去主导这个CodeAgent-CLI工具的核心开发。 > > 核心MVP大概做了两个多月，跑通了Agent调度、工具接入、流水线分析这些主流程。之后的大半年时间，一边做试点迭代，收badcase调优RAG和Prompt，补充新的Skill；一边也参与组里其他AI工具的配合工作，比如共用知识库的运营、其他工具的RAG调参。 > > 除此之外，也兼顾一部分内部工具的发布和日常运维支持，整体是核心项目为主，穿插其他效率工具开发的节奏。” ### 这样说的好处： 1. 时间线饱满，一整年的工作内容清晰，不会出现“一个工具做一年”的不合理感； 2. 职业路径自然：从运维工具→大模型应用开发，符合你的背景，不突兀； 3. 能力有广度：既懂业务场景，又懂Agent/RAG开发，还懂工程运维，形象更立体； 4. 核心突出：大部分时间和成果还是聚焦在CodeAgent上，不偏离求职目标。

<br />

> \# CodeAgent-CLI 最终版 · 简历+面试全材料 > 定位：1年左右大模型/后端开发经验，目标岗位「大模型应用开发 / RAG / Agent 开发」 > 核心原则：底层RAG归中台、本人负责业务侧核心编排与Agent链路；能力裁剪是主动工程权衡，而非技术短板；流水线分析为核心差异化场景；所有内容符合大厂内部工具真实分工，面试深挖不翻车。  \*\*\*  ## 一、工作经历整体表述（简历「工作经历」栏直接使用） > \*\*华为终端 · 研发工具 / 大模型应用开发岗\*\*  |  XX年XX月 - 至今 > > 负责部门内部研发效率类AI工具的开发与迭代，核心主导CodeAgent-CLI代码智能助手的Agent调度链路与RAG应用层实现，同时参与多工具共用知识库运营、运维效率工具开发等工作。 > > - 核心主导内部CLI代码助手的Agent引擎设计、工具接入层实现与RAG上下文编排，面向CI/CD流水线排查、代码规范查询等场景提效； > - 维护部门多AI工具共用的研发知识库，负责文档清洗、分类治理与检索效果调优，保障多工具知识数据源一致性； > - 开发多款运维自动化脚本与效率工具，覆盖流水线批量操作、环境部署、日志查询等场景，支撑研发日常流程提效； > - 跟进工具试点灰度与用户反馈，建立badcase分层归因机制，持续迭代优化工具效果与稳定性。  \*\*\*  ## 二、核心项目：CodeAgent-CLI 简历精简版 > （直接写入简历「项目经历」模块，180-280字，关键词密度适配ATS初筛） > > \*\*CodeAgent-CLI 代码智能助手 | 内部MVP试点项目\*\* > > 面向客户端研发团队的命令行AI辅助工具，为部门内部多套AI研发工具之一，当前小规模试点运行。作为核心开发，独立负责Agent调度引擎与RAG应用层全量设计实现，配合1名同事完成CLI交互层落地。 > > - 设计实现轻量Agent调度引擎，支持\`/\`快捷命令驱动的多模式路由（plan/build/流水线分析），不同模式自动加载对应Prompt、工具集与调用策略；搭建统一工具接入层，兼容自研Skill与标准MCP协议工具，内置调用管控与异常降级机制。 > - 重点落地流水线分析专用Agent，定向调度工具拉取构建日志、代码变更、排障文档等多源数据，聚合后输出故障定位结论，替代人工跨系统取数流程。 > - 实现多源上下文融合编排模块，完成检索片段、工具数据、会话历史的排序去重与token管控；建立检索置信度分级与多级兜底机制，抑制幻觉输出。 > - 完成200+篇内部文档的知识库治理，通过四层成本控制策略将单轮对话平均Token消耗降低40%+，沉淀badcase分层归因框架支撑持续迭代。  \*\*\*  ## 三、详细项目底稿（面试口述用，提前熟悉逻辑） ### 1. 项目定位与背景 这是部门内部的MVP试点工具，不属于正式商用产品，目标是验证AI工具对研发流程的提效价值。面向部门内200-400名客户端研发人员，当前在核心项目组小规模灰度试用。 项目硬约束：Minimax调用有token配额，成本敏感；无专职文档标注人力，内部历史文档碎片化；MVP优先跑通核心链路，复杂能力留待后续迭代。  ### 2. 团队分工与开发周期 - \*\*团队规模\*\*：核心开发共2人。我负责Agent调度引擎、工具接入层、RAG应用层、流水线场景链路、Prompt体系设计；另1名同事负责CLI基础框架、命令行解析、打包发布。底层RAG能力复用公司中台服务，流水线数据对接平台团队的MCP接口，无需从零搭建基础设施。 - \*\*开发周期\*\*：   1. 需求调研与技术选型：2周，对齐研发痛点，验证模型、中台RAG、MCP协议适配性，确定MVP范围；   2. 核心MVP开发：2.5-3个月，跑通Agent调度、工具接入、RAG编排、流水线分析主链路，产出可用版本；   3. 试点灰度与迭代优化：持续4-5个月，小范围试用收集反馈，迭代优化Prompt、补充Skill、修复边界问题，逐步扩大试点范围；   4. 剩余时间穿插其他工作：知识库运营、其他AI工具配合开发、运维效率工具开发。  ### 3. 技术架构与职责边界 | 层级 | 承担方 | 说明 | | :--- | :--- | :--- | | 文档解析/分块/向量存储/向量检索 | 公司中台团队（HTTP服务） | 不开发RAG底层内核 | | 大模型底座 | Minimax API，不做微调 | 仅做Prompt工程与业务编排 | | Agent调度引擎/工具接入/多模式路由 | 本人 | 业务侧核心逻辑实现 | | RAG上下文编排/置信度分级/兜底 | 本人 | 业务侧RAG核心价值 | | 流水线场景Agent链路 | 本人 | 核心业务场景落地 | | CLI交互框架/打包发布 | 协作同事 | 基础工程能力 |  ### 4. 核心实现细节 #### Agent侧核心实现 1. \*\*轻量Agent调度引擎\*\*：实现模式匹配、工具调度、调用管控的核心状态机。支持\`/\`快捷命令切换模式，不同模式自动加载对应系统Prompt、工具白名单、最大调用轮次，实现会话上下文隔离，避免模式串扰。 2. \*\*统一工具接入层\*\*：抽象统一的工具接口规范，对内适配自研Skill，对外适配标准MCP协议工具，上层调度无需感知底层差异；内置超时控制、错误重试、失败降级逻辑，保障工具调用链路稳定。 3. \*\*调用成本管控\*\*：实现token预算实时计算与调用轮次限制，每轮工具调用自动扣减预算，达到上限直接终止生成，避免成本不可控。  #### RAG侧核心实现 1. \*\*多源上下文融合编排模块\*\*：对RAG检索片段、工具返回结构化数据、会话历史按优先级排序，做去重、裁剪、拼接，在token预算内优先保留高价值信息，解决中台原始片段直接输入的冗余、超限问题。 2. \*\*检索置信度分级机制\*\*：基于检索相似度、关键词命中规则做置信度打分，低置信结果标记后送入模型，同时通过Prompt约束模型“信息不足则明确告知，禁止编造”，从工程+Prompt双层抑制幻觉。 3. \*\*业务知识库治理\*\*：建立文档准入、清洗、分类标准，针对开发规范、接口文档、排障手册不同类型文档配置差异化检索参数，持续迭代调优召回效果。  ### 5. 核心业务场景：流水线分析Agent - \*\*业务痛点\*\*：研发排查CI/CD失败时，需要跨流水线平台、代码仓库、文档库多个系统取数，人工排查效率低。 - \*\*实现链路\*\*：用户进入流水线模式并输入任务ID → Agent自动调度MCP工具拉取构建日志、失败节点、代码变更信息 → 同时调用RAG检索对应错误码排障手册 → 上下文编排模块聚合多源数据 → 模型输出结构化故障定位与修复建议。 - \*\*核心价值\*\*：把多步人工操作收敛为单命令完成，且所有结论基于真实数据，从源头减少幻觉，天然适配“有限轮次+成本可控”的MVP约束。  ### 6. 工程权衡与取舍 | 决策 | 原因 | 主动接受的代价 | | :--- | :--- | :--- | | 有限轮次toolcall，不做开放式ReAct循环 | token配额约束+MVP时效+场景明确 | 复杂多步任务覆盖有限 | | 复杂PDF/扫描件不纳入知识库 | 无专职标注人力+解析成本高 | 知识覆盖范围收窄 | | 不做模型微调，纯Prompt+RAG | 人力成本高+MVP优先验证价值 | 输出风格有一定波动 | | 上下文截断，不维护超长会话 | 成本控制+CLI终端阅读体验 | 长对话体验一般 | | 复用中台RAG能力，不自研底层 | 避免重复造轮子+聚焦业务价值 | 底层能力优化依赖中台排期 |  ### 7. 沉淀与后续规划 - 沉淀badcase分层归因框架，区分数据源、检索、后处理、Prompt四类问题，为后续迭代提供输入； - 后续迭代方向：扩大知识库覆盖范围、在成本可控前提下引入轻量重排优化检索效果、逐步放开部分场景的工具调用轮次、搭建最小化评估体系。  \*\*\*  ## 四、高频面试题库 > 格式统一：考察意图 → 参考答法 → 露馅点，覆盖90%以上面试高频问题  ### （一）项目定位与分工类 #### Q1 这个项目几个人做？你负责哪部分？ - \*\*考察意图\*\*：验证项目真实性，判断你的核心贡献占比，避免“蹭项目”。 - \*\*参考答法\*\*：核心开发两个人。我主要负责Agent调度引擎、统一工具接入层、流水线场景的Agent链路，还有RAG的上下文编排、Prompt体系和效果调优；另一个同事负责CLI基础框架、命令行交互和打包发布。底层RAG用的是公司中台的服务，流水线数据对接平台团队的MCP接口，我们专注在业务逻辑和场景落地上。因为是内部试点工具，没有专职产品和测试，需求来自研发真实痛点，边做边收反馈迭代。 - \*\*露馅点\*\*：说“全是我一个人做的”，或讲不清同事分工、依赖的外部团队。  #### Q2 这个项目做了多久？你一整年都在做这个吗？ - \*\*考察意图\*\*：判断产出效率，验证工作经历真实性。 - \*\*参考答法\*\*：核心MVP开发了两个半月到三个月，跑通了所有主流程；之后大半年时间一直在做试点迭代，收badcase调优Prompt、补充新的Skill、优化稳定性。除此之外我也参与组里其他AI工具的配合工作，比如多工具共用知识库的运营、其他代码助手的RAG调参，还有一些运维效率工具的开发，不是一整年只做这一个工具。 - \*\*露馅点\*\*：说“做了一整年”，或说不出其他工作内容，时间线空洞。  #### Q3 为什么不自己从零搭RAG，要用中台的？ - \*\*考察意图\*\*：考大厂分工思维，判断你是否理解业务开发和平台开发的边界。 - \*\*参考答法\*\*：一方面是避免重复造轮子，公司中台已经有成熟的RAG服务，文档解析、向量存储、检索这些基础能力都打磨过，从零搭既浪费人力也达不到中台的稳定性；另一方面是我们的核心价值在业务场景落地，不是做底层引擎，把精力放在怎么把检索能力和业务场景结合、怎么优化最终问答效果上，投入产出比更高。 - \*\*露馅点\*\*：答“中台的好用所以用”，讲不清分工价值；或反过来吹“我自己也能写”。  ### （二）RAG实现与价值类 #### Q4 RAG底层都是中台做的，你这块的技术含量体现在哪？ - \*\*考察意图\*\*：戳穿“调接口凑简历”的候选人，验证你对业务侧RAG价值的理解。 - \*\*参考答法\*\*：我的价值不在再造一个RAG引擎，而在业务侧的编排和效果落地。第一是多源上下文融合编排，中台返回的是零散的原始片段，直接塞给模型冗余多、容易超限，我做了排序、去重、裁剪，把工具返回的结构化数据、会话历史和检索片段按优先级整合，这直接决定生成质量；第二是置信度分级和兜底，低质量检索结果不让模型硬答，从工程层面减少幻觉；第三是业务知识库治理，哪些文档能进、怎么分类、不同文档配什么参数，这些都是决定效果的关键。底层引擎可以复用，但“怎么把检索能力用好”是业务侧的核心工作。 - \*\*露馅点\*\*：答“就是调了中台接口”，或反过来编造自己做了分块、向量检索。  #### Q5 badcase你怎么分层定位？举个例子 - \*\*考察意图\*\*：考问题排查能力，验证是否真的做过迭代优化。 - \*\*参考答法\*\*：我分四层定位：数据源、检索侧、后处理、Prompt。比如有用户反馈某类流水线问题回答笼统，我先看知识库有没有对应的排障文档，没有就是数据源问题；有文档但没召回，就是检索侧参数或者分类的问题；召回了但被后处理裁掉了，就是后处理的优先级策略有问题；上下文都对但模型没答到点上，就是Prompt约束不到位。一层层排除，不先甩锅给模型或者中台。 - \*\*露馅点\*\*：答“效果不好就改Prompt”，没有分层排查逻辑。  ### （三）Agent架构与实现类 #### Q6 你这个Agent具体实现了什么？不就是调用大模型的toolcall吗？ - \*\*考察意图\*\*：区分“调接口”和“做Agent开发”，验证核心实现深度。 - \*\*参考答法\*\*：大模型只提供了toolcall的基础能力，完整的Agent逻辑是业务侧实现的。首先我做了一层统一工具接入框架，把内部Skill和外部MCP服务都封装成标准格式，上层调度不用感知底层差异，新增工具也不用改核心逻辑；然后实现了多模式路由引擎，不同场景加载不同的工具集、Prompt和调用策略，比如流水线模式和普通问答模式的能力边界完全不一样；最重要的是面向流水线排查做了定向的执行链路，不是让模型自由乱调用，而是按业务逻辑定向拉取数据、聚合信息，既保证效果又控住成本。另外还有调用管控、异常降级、token预算这些生产级逻辑，也都是我实现的。简单说，模型给的是“能调用工具”的能力，我做的是“调用什么、怎么调、调完怎么处理、出问题怎么办”这一整套工程化逻辑。 - \*\*露馅点\*\*：说“就是用模型的function call功能”，讲不清自己实现的逻辑层。  #### Q7 为什么不做无限多轮ReAct循环，而是有限轮次？ - \*\*考察意图\*\*：考工程权衡思维，是否有成本意识和场景判断力。 - \*\*参考答法\*\*：主要三个原因。第一是成本约束，Minimax有token配额，无限循环调用成本不可控；第二是我们的核心场景比如流水线排查，本身有明确的取数路径，不需要开放式探索，定向编排的效果反而更稳定；第三是MVP阶段优先跑通核心价值，不需要追求通用Agent能力。有限轮次的好处是成本可预测、链路可审计、结果更稳定，更适合内部工具的落地场景。 - \*\*露馅点\*\*：答“ReAct不好用”，或说不出成本、场景层面的考量。  #### Q8 Skill和MCP有什么区别？为什么要同时支持两套？ - \*\*考察意图\*\*：考工具生态的理解，验证是否真的对接过多源工具。 - \*\*参考答法\*\*：Skill是我们自研的内部工具，定制化强、接入灵活，适合部门专属的业务能力；MCP是标准化协议，可以直接对接外部已有的工具服务，比如流水线平台的能力，不用重复造轮子。同时支持两套，就是兼顾内部定制化和外部复用效率。我在实现的时候做了一层统一的工具抽象，上层Agent不用关心底层是Skill还是MCP，新增工具只要适配接口就行，扩展性更好。 - \*\*露馅点\*\*：分不清两者边界，或说“MCP是新技术所以用”。  ### （四）业务场景与工程落地类 #### Q9 流水线分析模式和普通问答模式本质区别是什么？ - \*\*考察意图\*\*：验证是否理解核心业务场景，避免项目空泛无落点。 - \*\*参考答法\*\*：本质区别是知识来源不一样。普通问答模式依赖已有知识库，是“查存量文档”；流水线分析模式是实时从外部系统取数，是“拉实时数据+查文档”结合。所以调度逻辑也不一样，流水线模式会先自动调用工具拉取构建日志、代码变更、错误信息这些实时数据，再结合检索到的排障文档做分析，所有结论都基于真实数据，幻觉更少，解决的是研发跨系统取数效率低的痛点。 - \*\*露馅点\*\*：说“就是多了个模式名”，讲不清数据链路和业务价值。  #### Q10 检索后处理你做了什么？为什么不能直接用中台返回的结果？ - \*\*考察意图\*\*：考RAG业务侧的细节深度，验证是否真的做过后处理逻辑。 - \*\*参考答法\*\*：中台返回的原始chunk有三个问题：一是有重复片段，浪费token；二是部分低相关片段会干扰模型判断；三是总量可能超出上下文窗口。所以我做了四步处理：过滤低相关片段、去重相似内容、裁剪冗余信息、按优先级排序拼接，同时做长度截断保证不超限。这一步相当于把中台给的“原料”精加工成模型好用的“素材”，对最终效果影响很大。 - \*\*露馅点\*\*：答“直接拼起来就行”，说不出具体处理动作。  ### （五）成本与优化类 #### Q11 成本控制你具体做了哪些手段？ - \*\*考察意图\*\*：AI应用岗必考，验证是否有生产级成本管控意识。 - \*\*参考答法\*\*：我做了四层管控，从粗到细。第一层是调用侧，限制工具最大调用轮次，控制检索top-N数量，减少输入token；第二层是上下文侧，做多源内容的去重裁剪和优先级排序，在预算内保留最高价值信息，同时截断长会话历史；第三层是输出侧，用max\_tokens限制输出长度，配合Prompt强约束输出简短，适配CLI的同时省token；第四层是模式侧，不同场景配置不同的token预算，高价值场景配额多一点，普通问答收紧。核心思路是成本要显式管控，不能事后看账单。 - \*\*露馅点\*\*：只答“设了max\_tokens”，讲不出多层级的管控逻辑。  #### Q12 这个项目效果怎么样？有没有量化数据？ - \*\*考察意图\*\*：验证成果真实性，避免候选人编造虚假指标。 - \*\*参考答法\*\*：因为是内部MVP，还没有搭建完整的自动化评估体系，所以我不会随便报召回率、准确率这种没有实测支撑的指标。我能说的是过程性的结果：第一是核心场景跑通了，流水线排查场景能替代人工跨系统取数的步骤，研发反馈排查效率有提升；第二是成本控制达标了，通过优化把单轮平均token消耗降了40%以上，在配额内可以支撑试点规模；第三是沉淀了八十多条badcase和分层归因方法，为后续迭代打好了基础。后续如果扩大试点，会考虑搭建最小化的评估体系。 - \*\*露馅点\*\*：临时编造“召回率提升XX%”“准确率XX”，一问细节就崩。

# 一、项目基础信息（内部共识，勿在简历中过度展开）

- **项目名称**：CodeAgent-CLI 代码智能助手（华为客户端业务二级部门，内部多个 AI 工具之一，非独立产品，MVP 试点）。
- **面向用户**：客户端部门研发人员，潜在 200–400 人，当前小规模试点。
- **部署形态**：CLI 命令行工具，`/` 快捷命令触发能力。
- **模型底座**：Minimax API，不微调，仅 Prompt 工程 + 业务侧 RAG 编排 + 轻量 Agent。
- **RAG 底座**：公司中台团队提供 RAG HTTP 服务（解析、分块、向量存储、检索），本人不开发底层内核。
- **本人职责**：独立负责 CLI 工具的业务侧 RAG 应用层、Agent 调用链路、Skill 编排、MCP 对接、多模式调度。

## 二、硬性约束（绝对不能写/吹的能力）

❌ 向量检索引擎、文档分块逻辑、搭建向量库、BM25 混合检索、Rerank 重排序、语义层级分块、OCR/表格解析、知识库自动增量更新、hash 变更检测、soft-delete 版本管理、多租户 ACL 权限、完整离线评估流水线、LLM-as-Judge、多轮 Agent 反思循环、自我校验。

***

# ① 简历简短项目经历（180–280 字）

> **CodeAgent-CLI 代码智能助手｜华为客户端业务 · 内部 MVP 试点**

独立负责 CLI 工具的业务侧 RAG 编排与 Agent 链路。面向部门研发人员，基于 Minimax 大模型 API 与公司中台 RAG 服务，实现 `/` 快捷命令驱动的多模式调度（plan / build / 流水线分析）。RAG 侧负责数据源治理、检索参数调优（top-N 与 token 开销控制）与检索后处理（过滤、去重、裁剪、上下文拼接防超限），设计 Prompt 约束检索使用范围、抑制幻觉，并兜底无结果分支。Agent 侧打通自研 Skill 与外部 MCP 协议工具，实现有限轮次 toolcall 的轻量弱 Agent，通过系统 Prompt + max\_tokens + 上下文截断控制成本、适配 CLI 阅读。重点建设流水线分析模式，联动 Skill/MCP 拉取 CI/CD 原始数据后做定向分析；沉淀 badcase 分层定位机制，为后续迭代提供依据。

***

# ② 详细项目描述（面试底稿）

### 1. 项目定位与约束

- **性质**：部门内部多个 AI 开发工具之一，非独立产品，MVP 试点、未全量推广。目标用户 200–400 名研发，当前小规模灰度。
- **硬约束**：Minimax 调用有 token 配额（成本敏感）；无专职人力做全量文档清洗标注，历史文档碎片化；只求跑通业务链路，复杂能力留后续。

### 2. 技术架构与职责边界（关键，明确"我做什么、不做什么"）

| 层                              | 承担方                 | 说明                              |
| ------------------------------ | ------------------- | ------------------------------- |
| 文档解析 / 分块 / 向量存储 / 向量检索        | **公司中台团队**（HTTP 服务） | 我不开发 RAG 底层内核                   |
| 模型底座 / 微调                      | Minimax API，**不微调** | 仅 Prompt 工程                     |
| 业务侧 RAG 编排                     | **本人**              | 数据源治理、参数调优、检索后处理、Prompt、badcase |
| Agent 链路 / Skill / MCP / 多模式调度 | **本人**              | 工具扩展、toolcall 编排、模式路由           |
| CLI 交互层                        | **本人**              | `/` 快捷命令、终端阅读体验                 |

**一句话定位**：在"底层 RAG 已由中台提供、模型不微调、配额有限"的既有条件下，负责把 RAG + Agent 编排成研发人员能直接用的 CLI 工具。

### 3. RAG 业务侧（我做的五件事）

1. **数据源治理**：筛选部门 wiki、开发规范、接口文档；对原文清洗、过滤无效噪声后交付中台入库；复杂 PDF/扫描件**主动排除**——在无专职标注和算力配额下，宁要"少而干净"的数据，不要"多而脏"的数据。
2. **调用中台 + 调参**：调 top-N 召回数量，在"召回覆盖率"和"token 开销"之间取平衡；控制返回片段规模。
3. **检索后处理**：对中台返回 chunk 做过滤、去重、裁剪；设计上下文拼接 + 截断策略，避免输入超限。
4. **Prompt 设计**：明确约束模型"如何使用检索上下文"，抑制幻觉；对检索无结果设计独立兜底分支（明确回答"未找到"而非硬编）。
5. **badcase 分层定位**：区分问题出在**数据源 / 检索侧 / 后处理 / Prompt** 哪一层，做针对性迭代，不把锅甩给模型或中台。

### 4. Agent & 工具链路（重点新增）

- **CLI 交互**：`/` 快捷命令入口，切换模式、触发 Skill。
- **工具体系**：同时支持**自研 Skill** 与**外部 MCP 协议服务**，形成可扩展工具集。
- **多模式调度**：plan、build、以及**流水线分析专用模式**（面向 CI/CD 排查，当前重点建设方向）。
- **轻量弱 Agent**：基于 Minimax toolcall，**最多单轮/有限轮次**工具调用，**无无限 Thought-Action-Observation 循环**。
- **成本控制三重手段**：系统 Prompt 强约束输出简短（适配 CLI）；max\_tokens 限制输出长度；会话做上下文截断、不维护超长多轮。

### 5. 核心业务场景：流水线分析 Agent（重点讲）

- **痛点**：研发排查 CI/CD 失败时需要多方拉数据（流水线日志、错误摘要、代码变更、构建产物），人工跨系统取数效率低。
- **我做的**：流水线分析模式下，Agent 按策略调用 Skill/MCP 拉取流水线原始数据（而非让模型凭空猜→抑制幻觉），拿到结构化/原始数据后再做定向分析，输出简短的定位结论。
- **为什么这是重点**：它最能体现"Agent + 外部工具 + 有限轮次 toolcall"的价值——**模型负责分析，工具负责取数**，两者分工明确，且天然适配"弱 Agent + 成本控制"的约束。

### 6. 工程权衡决策记录（面试的高分素材）

| 决策                           | 原因                     | 主动接受的代价                |
| ---------------------------- | ---------------------- | ---------------------- |
| 不做无限多轮 Agent，只做有限轮次 toolcall | token 配额 + 成本 + MVP 时效 | 复杂多步任务覆盖有限             |
| 复杂 PDF/扫描件不纳入知识库             | 无专职清洗标注、解析成本高          | 覆盖范围收窄                 |
| 不微调模型，纯 Prompt + RAG         | 配额/人力约束，MVP 优先         | 输出风格/格式靠 Prompt 约束，有波动 |
| 上下文截断、不维护超长会话                | 控制成本 + 终端阅读            | 长对话场景体验一般              |
| badcase 只做分层定位，不上自动化评估流水线    | 缺标注数据 + MVP 阶段         | 效果验证偏人工、偏粗             |

**现象印证（诚实讲现状）**：普通问答输出简短、部分问题回答笼统；流水线模式下依赖 Skill/MCP 拿数据后再分析。这些不是缺陷，是**约束下主动做的能力裁剪**。

### 7. 沉淀与后续

- 沉淀 badcase 分层框架和一批负样本，作为后续引入评估、优化检索/分块的**输入基础**。
- 后续迭代方向：扩大数据源治理、引入针对性评估、按配额成本逐步放开轮次。

***

# ③ 高频面试问答

### Q1【陷阱】RAG 底层（解析、分块、向量）都是中台做的，那你这块的"技术含量"体现在哪？

- **考察意图**：戳穿"简历写着 RAG、其实是调用现成服务"的候选人，考你是否清楚自己的**真实增量**。
- **参考回答**：我的增量不在"再造一个 RAG"，而在于**业务侧编排**：① 数据源治理——分清什么文档该进知识库、怎么清洗降噪；② 检索后处理——中台返回的 chunk 不能直接塞给模型，要做去重/裁剪/拼接/截断，这直接决定生成质量；③ Prompt 约束——让模型知道怎么用上下文、抑制幻觉、兜底无结果；④ badcase 分层归因。底层能力可以被中台替代，但"什么数据该进、检索结果怎么用、模型怎么被约束"这层业务编排，是真正决定效果的地方。
- **露馅点**：答"我就调了中台一个接口"或反过来吹"我实现了向量检索"。

### Q2 为什么不做无限多轮 Agent（ReAct 循环），而是有限轮次 toolcall？

- **考察意图**：考工程判断——你是否会为技术先进性牺牲成本，是否有成本意识。
- **参考回答**：三点原因：① Minimax 配额有限，无限循环 token 不可控；② 内部 MVP 要的是"先跑通业务链路"，不是追求通用 Agent 能力；③ 我的核心场景（流水线分析）本身就有明确的工具调用路径，不需要开放式探索。所以设计成"有限轮次 + 明确工具"，成本可控、可预测、好审计。
- **露馅点**：答"因为 ReAct 太慢/过时了"——没落到成本和场景，说明是背概念。

### Q3 成本控制你具体做了哪些手段？优先级怎么排？

- **考察意图**：AI 应用岗必考成本，考你是否"显式管理成本"。
- **参考回答**：四道闸门，从粗到精：① **输出侧** max\_tokens 限制长度；② **Prompt 侧**强约束输出简短、贴合 CLI；③ **上下文侧**截断、不维护超长会话；④ **调用侧**有限轮次 toolcall + top-N 控制检索 token。核心思路是**成本是显式约束，不是事后看账单**。
- **露馅点**：只答"设了 max\_tokens"，说不出其他层，说明理解停留在单个参数。

### Q4 流水线分析模式是怎么调度的？它和普通问答模式本质区别在哪？

- **考察意图**：考你能否讲清核心场景的设计，这是本项目最大亮点。
- **参考回答**：本质区别在\*\*"数据从哪来"\*\*——普通问答是"检索已有知识库"；流水线分析是"实时从外部系统取数"。所以调度上：先识别用户意图进入流水线模式 → 按策略调用 Skill/MCP 拉取流水线原始数据（日志、错误摘要、变更信息）→ 拿到数据后再让模型做定向分析。这样模型是"基于真实数据分析"，而不是凭空猜，从源头抑制幻觉。
- **露馅点**：答"就是多了个模式"或说不出数据获取链路，说明没理解区别。

### Q5 Skill 和 MCP 有什么区别？为什么要同时支持两套？

- **考察意图**：考工具生态理解（MCP 是 2026 高频，但要答得务实）。
- **参考回答**：Skill 是**自研内部工具**，接入快、定制强，适合部门专属能力（如内部系统查询）；MCP 是**标准工具协议**，能复用外部已有的工具服务，避免重复造轮子。同时支持是为了"内部能力可控可定制 + 外部能力快速复用"两条腿走路。我用统一的工具注册/调用抽象，上层 Agent 不关心底层是 Skill 还是 MCP。
- **露馅点**：答"MCP 更新所以用 MCP"或分不清两者边界。

### Q6 检索后处理你做了什么？为什么不能把中台返回的 chunk 直接塞给模型？

- **考察意图**：考业务侧 RAG 的具体价值，验证细节。
- **参考回答**：中台返回的是原始 chunk，直接塞有三个问题：① 重复片段浪费 token；② 无关片段干扰模型；③ 总量可能超上下文窗口。所以做：过滤（去明显无关）、去重（相似/重复 chunk）、裁剪（取关键片）、拼接 + 截断（保证不超限且优先高置信片段）。这层是"中台给原料，我精加工"。
- **露馅点**：答"直接拼起来就行"或说不出后处理具体动作。

### Q7 badcase 你怎么分层定位？举一个真实例子。

- **考察意图**：考归因能力和是否真迭代过。
- **参考回答**：分四层——**数据源 / 检索侧 / 后处理 / Prompt**。举例（用你的真实方向）：某类问题回答笼统，先看是不是数据源里根本没这块资料（数据源问题）；有资料但没召回（检索侧 top-N 太小）；召回了但被后处理裁掉了（后处理问题）；上下文都对了但模型没利用好（Prompt 约束问题）。逐层排除，不先甩锅给中台或模型。
- **露馅点**：答"效果不好我就换了个模型/改了 prompt"——没有分层意识。

### Q8 上下文截断怎么做？会不会切掉关键信息？

- **考察意图**：考上下文管理细节。
- **参考回答**：按"近轮优先 + 关键信息保留"策略截断：保留最近的对话和当前任务的必要上下文，旧历史压缩或丢弃；同时靠检索上下文和工具返回数据补足信息。代价是长对话体验打折，但这在 MVP 和成本约束下是**主动取舍**。切掉关键信息的风险，靠"检索/工具拉取"兜底，而不是靠无限堆历史。
- **露馅点**：答"就是把超长的截掉"——没有策略，也没意识到权衡。

### Q9【陷阱】为什么不直接调 Minimax 联网/长上下文，非要自己做 RAG 编排？

- **考察意图**：考技术选型判断（是否真理解为什么用 RAG）。
- **参考回答**：因为知识是**部门私有/内部**的（wiki、规范、接口文档），模型没有且不能外传；且内部有中台 RAG 服务，复用成本最低。自己做的是"把内部知识按需喂给模型"这层编排，而不是重复造底层的轮子。
- **露馅点**：答"因为 RAG 更高级"。

### Q10 数据源治理：为什么复杂 PDF/扫描件不纳入？这算不算能力不足？

- **考察意图**：考你是否能把"约束下的裁剪"讲成正向决策。
- **参考回答**：不是能力不足，是**性价比决策**——解析复杂 PDF/扫描件成本高（OCR、版面还原），而内部无专职标注人力、算力配额又有限，把资源花在"少而干净"的高价值文档上，比追求"全而脏"更符合 MVP 目标。后续资源到位再扩容源类型。
- **露馅点**：答"因为解析不了"或支支吾吾承认是短板。

### Q11 这个项目你最大的工程权衡是什么？

- **考察意图**：开放性总结题，看你能否一句话提炼项目的核心决策逻辑。
- **参考回答**：**能力 vs 成本的显式取舍**。在"配额有限 + 无专职标注 + MVP 时效"三重约束下，我选择"有限轮次 Agent + 精简数据源 + 强 Prompt 约束"——牺牲了通用性，换来了可控、可预测、能落地的链路，并用 badcase 沉淀为后续迭代留口子。核心不是"做得全"，而是"在约束下做得对"。
- **露馅点**：答不出一个统一主线，各条孤立讲。

### Q12 简历里没写量化指标，面试官问"效果怎么样"你怎么答？

- **考察意图**：考你能否用"定性 + 过程"替代虚构数字，且不心虚。
- **参考回答**：坦诚讲——这是内部 MVP，没有建立自动化评估，所以我**不编召回率**。我讲的是：哪些真实场景跑通了（流水线分析能拿到数据做定位）、哪些 badcase 定位到了具体层、沉淀了什么。效果好不好我可以讲"对比人工排查的路径"，但不给没有实测支撑的数字。这反而比报假数字更可信。
- **露馅点**：被一问就临时编一个百分比。

