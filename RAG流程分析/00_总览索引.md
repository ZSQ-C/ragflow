# RAGFlow RAG & Agent 实现流程详解 —— 文档索引

> 本系列文档按 RAGFlow 三层 RAG 架构逐步拆解，每份文档包含八大模块：核心总览、模块拆分、方法详细解析（5要素+文字串讲）、同类对比、疑惑解答、规范修正、实操步骤、关键模块总览。

---

## 📚 文档列表

### 第一层：离线索引（文档入库）

| 序号 | 文档 | 核心内容 | 对应源码 |
|------|------|----------|----------|
| — | *(参见 RAGFlow_RAG_Agent_实现流程详解.md)* | 格式检测、解析器选择、OCR、布局识别、分块、向量化 | `rag/app/naive.py`、`deepdoc/`、`rag/nlp/__init__.py` |

### 第二层：在线检索（用户提问）

| 序号 | 文档 | 核心内容 | 对应源码 |
|------|------|----------|----------|
| **01** | [01_async_chat_RAG流程总调度.md](./01_async_chat_RAG流程总调度.md) | RAG 全链路编排：路由→模型获取→问题精炼→检索→Prompt→LLM | `api/db/services/dialog_service.py` L455-L781 |
| **02** | [02_混合检索与精排全链路_Dealer.md](./02_混合检索与精排全链路_Dealer.md) | `Dealer.search()` 混合检索（三分支+降级）+ `Dealer.retrieval()` 精排（粗排1024→精排64→过滤→聚合）+ `Dealer.rerank()` / `rerank_by_model()` | `rag/nlp/search.py` L36-L521 |
| **04** | [04_引用溯源与查询构造.md](./04_引用溯源与查询构造.md) | `FulltextQueryer.question()` 全文查询构造（中英文、细粒度分词、同义词）+ `insert_citations()` 引用溯源（迭代阈值降级） | `rag/nlp/query.py` L41-L168、`rag/nlp/search.py` L177-L267 |

### 第三层：生成回答

| 序号 | 文档 | 核心内容 | 对应源码 |
|------|------|----------|----------|
| **01** | [01_async_chat_RAG流程总调度.md](./01_async_chat_RAG流程总调度.md) | Prompt 构建（kb_prompt+citation_prompt+message_fit_in）+ LLM 流式生成（_stream_with_think_delta+SSE） | `dialog_service.py` L638-L781 |

### Agent 编排引擎

| 序号 | 文档 | 核心内容 | 对应源码 |
|------|------|----------|----------|
| **03** | [03_Agent编排引擎_Canvas与Agent.md](./03_Agent编排引擎_Canvas与Agent.md) | `Canvas.run()` 执行引擎（4阶段+路径状态机+事件系统）+ `Agent._invoke_async()`（工具绑定+3分支路由+流式）+ `LLMToolPluginCallSession.tool_call_async()`（3类工具适配） | `agent/canvas.py` L283-L667、`agent/component/agent_with_tools.py` L73-L378、`agent/tools/base.py` L50-L77 |

---

## 🔗 三层 RAG 架构的代码映射

```
第一层：离线索引
  文档上传 → 格式检测 → 解析器选择 → OCR识别 → 分块 → Embedding → 存储
  ├─ rag/app/naive.py#chunk       L729    文档分块总入口
  ├─ rag/app/naive.py#PARSERS     L254    解析器字典（5种PDF引擎）
  ├─ deepdoc/parser/pdf_parser.py          RAGFlowPdfParser（2057行，最复杂）
  ├─ deepdoc/vision/ocr.py                 OCR检测+识别
  ├─ deepdoc/vision/layout_recognizer.py   布局识别（11种标签）
  ├─ deepdoc/vision/table_structure_recognizer.py 表格结构识别
  ├─ rag/nlp/__init__.py#naive_merge  L1070  文本分块合并
  ├─ rag/nlp/search.py#get_vector      L52    Embedding向量化
  └─ common/doc_store/                        ES/Infinity/OB 存储

第二层：在线检索
  用户提问 → 查询优化 → 混合检索 → 降级重试 → 重排序 → 阈值过滤 → 聚合
  ├─ dialog_service.py#async_chat    L455    RAG总调度
  ├─ dialog_service.py#full_question L538    多轮合并
  ├─ dialog_service.py#cross_languages L543  跨语言翻译
  ├─ dialog_service.py#keyword_extraction L556 关键词增强
  ├─ search.py#search                L74     混合检索（全文+向量）
  ├─ search.py#search#FusionExpr      L127    weighted_sum 0.05:0.95
  ├─ search.py#search#降级重试        L136    空结果降min_match/similarity
  ├─ search.py#retrieval              L364    精排全链路
  ├─ search.py#rerank                 L270    本地多因子排序
  ├─ search.py#rerank_by_model        L335    外部模型排序（16种）
  ├─ query.py#question                L41     全文查询构造
  ├─ term_weight.py#weights                   IDF×NER×POS词权重
  └─ synonym.py#lookup                 L35    三级同义词查找

第三层：生成回答
  构建Prompt → Token裁剪 → LLM流式生成 → 引用插入 → SSE返回
  ├─ dialog_service.py#kb_prompt     L638    知识块格式化
  ├─ generator.py#kb_prompt                  格式化函数实现
  ├─ dialog_service.py#citation_prompt L653   引用要求注入
  ├─ dialog_service.py#message_fit_in L656    Token裁剪（95% max_tokens）
  ├─ dialog_service.py#async_chat_streamly_delta L751  LLM流式调用
  ├─ dialog_service.py#_stream_with_think_delta      思考标签解析
  ├─ search.py#insert_citations       L177    引用后处理（迭代阈值降级）
  └─ conversation_app.py#stream       L224    SSE响应包装
```

---

## 📊 方法调用关系图

```
HTTP POST /v1/conversation/completion
  │
  └─► conversation_app.completion()        [api/apps/conversation_app.py#L169]
        │
        ├─► ConversationService.get_by_id()   获取会话记录
        ├─► DialogService.get_by_id()          获取对话配置
        └─► async_chat(dia, msg, stream)      [api/db/services/dialog_service.py#L455]
              │
              ├─ get_models(dialog)            获取chat/embed/rerank/tts模型
              │   └─► LLMBundle(tenant_id, config) [llm_service.py]
              │
              ├─ full_question()               多轮对话优化
              ├─ cross_languages()             跨语言翻译
              ├─ keyword_extraction()          关键词提取
              │
              ├─ settings.retriever.retrieval()  核心检索
              │   │
              │   ├─ Dealer.search()           [rag/nlp/search.py#L74]
              │   │   ├─► FulltextQueryer.question()  [query.py#L41]
              │   │   │   ├─► term_weight.weights()   [term_weight.py]
              │   │   │   └─► synonym.lookup()        [synonym.py]
              │   │   ├─► Dealer.get_vector()         [search.py#L52]
              │   │   │   └─► emb_mdl.encode_queries()
              │   │   └─► FusionExpr("weighted_sum")
              │   │       └─► dataStore.search()      [ES/Infinity]
              │   │
              │   ├─ Dealer.rerank_by_model()  [search.py#L335]
              │   │   └─► rerank_mdl.similarity()
              │   │
              │   └─ Dealer.rerank()           [search.py#L270]
              │       └─► hybrid_similarity()
              │
              ├─ kb_prompt(kbinfos, max_tokens)
              ├─ message_fit_in(msg, max_tokens*0.95)
              ├─ chat_mdl.async_chat_streamly_delta()
              │   └─► _stream_with_think_delta()
              │
              └─► (yield答案块给SSE)
```

---

## 🎯 Agent 调用关系图

```
前端发送消息
  │
  └─► Canvas(dsl).run(query="...")          [agent/canvas.py#L375]
        │
        ├─ Graph.load()                      DSL反序列化
        │   └─► component_class("Agent")    工厂模式创建组件
        │
        ├─ _run_batch(f, t)                  批量并行执行
        │   └─► asyncio.gather(              Semaphore(5)控制并发
        │       ├─► Agent._invoke_async()    [agent_with_tools.py#L188]
        │       │   │
        │       │   ├─ LLM._invoke_async()   无工具→纯对话
        │       │   │
        │       │   ├─ partial(stream_output...)  流式输出
        │       │   │   ├─► full_question()      多轮优化
        │       │   │   ├─► _generate_streamly()  LLM流式
        │       │   │   │   └─► tool_choice → LLMToolPluginCallSession
        │       │   │   │       ├─► MCP工具 → thread_pool_exec
        │       │   │   │       ├─► 异步工具 → await
        │       │   │   │       └─► 同步工具 → thread_pool_exec
        │       │   │   └─► _gen_citations_async() 引用插入
        │       │   │
        │       │   └─ _generate_async()           非流式
        │       │       └─► json_repair.loads()    结构化输出
        │       │
        │       ├─► Retrieval._invoke_async()   检索工具
        │       └─► Begin.invoke()              入口组件
        │       )
        │
        └─ 后处理 (Message → 流式输出 → 事件推送)
            ├─► yield "node_started"
            ├─► yield "message" (流式内容 + TTS)
            ├─► yield "message_end" (引用 + 附件)
            └─► yield "workflow_finished"
```

---

## 🏷️ 快速命令索引

| 场景 | git grep 命令 |
|------|--------------|
| 找 RAG 入口 | `git grep "async def async_chat"` |
| 找混合检索 | `git grep "FusionExpr"` |
| 找引用插入 | `git grep "insert_citations"` |
| 找 Agent 执行 | `git grep "async def run" agent/` |
| 找 Tool Call | `git grep "tool_call_async" agent/` |
| 找词权重 | `git grep "def weights" rag/nlp/term_weight.py` |
| 找同义词 | `git grep "def lookup" rag/nlp/synonym.py` |
| 找降级检索 | `git grep "min_match=0.1" rag/nlp/search.py` |
