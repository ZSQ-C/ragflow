```
# RAGFlow的rag模块全量深度拆解分析提示词（零遗漏·全方法覆盖·零基础专属版）
## 核心指令
你需要对RAGFlow项目的`rag/`目录全量代码（含所有子目录下的全部.py文件），完成**100%全方法覆盖、零黑盒、无跳步、可复现、带设计亮点解读**的深度拆解分析，辅助参考`deepdoc/`文档解析模块，严格遵守以下所有规则输出，违者内容直接无效。

---

## 顶层不可突破铁律（100%严格遵守，无任何例外）
1.  **全方法100%覆盖强制要求**：对`rag/`目录下所有.py文件中的**每一个类构造方法、实例方法、静态方法、类方法、独立函数、内部嵌套函数、私有方法（_开头）**，必须做到100%全覆盖拆解，不得遗漏、不得跳步、不得只挑选核心方法、不得合并多个方法到同一个串讲中。
2.  **单方法独立串讲强制要求**：每个方法必须有专属的、独立的【方法文字流程串讲】，哪怕是仅3行代码的简单工具函数，也必须单独拆解；仅当方法为一行代码的极简getter/setter/常量返回函数时，可合并到所属文件的【文件级全局串讲】中，但必须单独列出方法名、核心功能、极简串讲，严禁直接省略。
3.  **功能+亮点双必填要求**：每个方法的串讲中，必须同时包含「核心功能定位」和「设计亮点/优化点」两个固定模块，哪怕是简单函数，也必须讲清「这个方法解决了什么业务/技术问题」和「这么写的设计优势/优化点是什么」，禁止只讲流程不讲价值。
4.  **零基础友好强制要求**：所有大模型/RAG专业术语首次出现必须加【通俗解释】，所有逻辑必须零黑盒、无跳步，看完就能理解每一行代码的作用，照着就能1:1复现功能。
5.  **反简略硬约束**：禁止用一句话带过一个方法，禁止用列表碎片化罗列步骤替代连贯文字串讲，每个非极简方法的串讲内容不得少于100字，最终输出总篇幅不得少于3000行，严禁生成1000行以内的简略内容。
6.  **顺序一致性要求**：文件、类、方法的拆解顺序，必须和代码仓库中实际的文件顺序、代码书写顺序完全一致，不得乱序、不得调换结构。

---

## 强制输出固定结构（绝对不可调换顺序）
必须严格按照「全局认知→目录级模块→文件级→方法级→对比总结→实操落地」的递进逻辑，按以下固定顺序输出，禁止调换结构、遗漏模块：

### 一、RAG模块全局总览
1.  核心定位：用2-3段连贯文字，讲清`rag/`模块在整个RAGFlow项目中的核心定位、完整业务功能、适用场景、解决的核心行业痛点，明确区分离线知识库构建和在线问答响应两大核心链路的边界。
2.  全链路执行生命周期总图谱：用连贯文字梳理从「用户上传文档→文档解析分块→向量化入库→用户发起提问→查询优化→混合检索→重排过滤→Prompt构建→LLM生成→引用插入→结果返回」的完整端到端执行链路，明确标注每个环节对应的目录/文件/方法，讲清各模块的执行先后顺序、上下游调用关系、每一步的核心目标。

### 二、目录级模块拆分与依赖关系总览
针对`rag/`目录下的每一个子目录（nlp、prompts、llm、app、utils、agents等），逐个明确：
1.  子目录核心定位、在RAG全链路中归属的阶段（离线/在线/全局通用）
2.  上游依赖的其他模块、下游被哪些模块调用
3.  子目录内的核心文件清单，以及每个文件的核心职责
4.  该子目录在整个RAG模块中的不可替代价值

### 三、文件级逐文件深度拆解（核心主体章节，100%文件覆盖）
针对`rag/`目录下的**每一个.py文件**，按子目录分类，逐个完成以下固定结构的拆解，每个文件单独成二级章节，文件内的每个方法单独成三级章节，严格遵守顶层铁律：
```

## 【文件完整路径】例：rag/nlp/search.py

### 1. 文件全局定位与串讲

- 一句话明确：该文件归属的目录级模块、在RAG全链路中的核心位置、解决的核心问题、上下游依赖的文件/模块
- 连贯文字讲清文件内的类、方法的组织逻辑、调用顺序、整体设计思路
- 极简方法合并说明（如有）：列出所有一行代码的极简方法，格式为「方法名：核心功能 + 极简串讲」，不得遗漏

### 2. 类/方法逐一枚举深度拆解（严格按代码书写顺序，100%全覆盖）

#### 【完整方法名】例：Dealer.**init**()

##### 方法文字流程串讲（强制必填）

1. 核心功能定位：先讲清这个方法在全链路中的触发时机、核心解决的问题、在所属流程中的角色
2. 全流程连贯讲解：用完整段落文字，描述方法从入参接收到执行完成的全流程，每一步操作的目的是什么；所有分支判断必须100%覆盖，明确「满足XX入参/状态条件→执行XX操作→实现XX目标；不满足条件→执行XX兜底逻辑→适配XX场景」
3. 设计亮点/优化点：专门一段，讲清这个方法的设计巧思、性能优化、容错处理、扩展性设计、RAG场景专属优化，比如为什么用单例模式、为什么用线程池、为什么加缓存、解决了什么行业痛点/技术问题
4. 边界与异常处理：讲清所有异常捕获、边界场景的处理逻辑，以及这么设计的原因，避免什么风险

##### 强制5要素结构化输出（强制必填）

| 要素名称   | 详细内容                                  |
| :----- | :------------------------------------ |
| 入参     | 每个参数的类型、必填/选填、默认值、参数的业务含义，禁止只写参数名     |
| 核心逻辑   | 1-2句话精简总结该方法的核心业务逻辑与设计目标              |
| 输出形式   | 返回值的类型、格式、业务含义；异常场景的返回规则（空值/报错信息/兜底值） |
| 底层关键依赖 | 该方法核心依赖的第三方模块、原生API、项目内其他文件/方法        |
| 关键代码片段 | 精简后的、可直接运行的核心代码，标注代码对应的执行环节，禁止残缺代码    |

##### 特殊处理标注（强制必填）

高亮标注该方法中的编码规范、异常捕获、格式转换、边界判断、RAG专属优化、性能优化、安全校验等细节，讲清该处理的设计原因，以及能解决的具体问题。

```

### 四、RAG同类功能逻辑全量对比表
针对项目中出现的所有同类可替换功能、多分支适配逻辑，用表格做汇总对比，必须覆盖但不限于：
- 多格式文档解析方法、多分块策略、多检索方式、多向量化模型适配、多LLM调用方式、多重排序策略、多异常降级方案
- 表格固定列：**功能名称、核心执行流程、核心入参、底层依赖API、输出格式、适用场景、核心优势、局限性**

### 五、入门级全量疑惑解答与避坑指南
1.  针对每个模块、每个核心方法，主动补充零基础入门开发者大概率会遇到的高频疑问、踩坑点，比如「为什么这个方法要设置这个默认值」「这个参数调大会有什么影响」「为什么检索不到结果大概率是这个方法出了问题」
2.  每个疑问必须给出**原因分析+对应代码中的优化点+可直接落地的解决方案**，禁止空泛讲解
3.  针对RAG全链路的常见问题（幻觉、检索不精准、引用错误、长文本溢出等），结合代码实现，讲清根因和优化方向

### 六、零基础可复现全流程实操步骤
严格按照RAG模块的完整执行流程，拆分「环境部署→离线知识库构建全流程→单方法调试→在线问答功能调试→全链路功能测试→常见问题排查」阶段，生成傻瓜式分步指南，每一步必须包含：**操作内容+依赖的文件/方法+最简可直接运行的代码+注意事项+执行目标**，保证零基础开发者照着步骤操作，就能100%复现完整的RAG功能。

### 七、全量核心依赖与方法索引总表
1.  汇总项目所有核心第三方依赖，固定列：**依赖名称、负责核心功能、在RAG全链路中的定位、入门选型建议**
2.  生成全量方法索引表，按文件分类，列出所有方法名、核心功能、所属流程阶段，方便开发者快速查找定位
```

